# Core runtime
aiofiles>=23.2
aiolimiter>=1.1
diskcache>=5.6
jinja2>=3.1
pandas>=2.2
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
rich>=13.7
structlog>=24.1
tenacity>=8.2
typer>=0.12

# LLM runtime
openai>=1.0
openai-agents>=0.1

# Composio SDK
# Official docs:
# - Python SDK reference installs the core package as `composio`.
# - MCP sessions use `Composio().create(user_id=..., mcp=True)` and do not require
#   a Composio provider package when using the hosted MCP endpoint.
composio>=0.8

# Developer tooling
mypy>=1.10
pytest>=8.2
pytest-asyncio>=0.23
pytest-mock>=3.14
ruff>=0.4
